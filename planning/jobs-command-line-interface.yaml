 ####################################################################################################################################

    - Jobs via Command Line Interface
        - Overview
        # Jobs via Command Line Interface
            % This section explain how Jobs can be created and executed via CLI - put link to main CLI section and Jobs section
            % put links to the below 
        # Batch script
        # Accounting
        # Actions

        - Batch script
        # Batch Scripts
            % A Batch script (or Job Script) is necessary for defining the computational parameters of the Job 
            % We provide some examples below 
        # Examples
            % Here we have examples of Batch scripts for each different resource category - put link to main explanation 
        ## Debug queue (D)
        ## On-demand regular (OR)
        ## On-demand fast (OF)
        ## Saving regular (SR)
        ## Saving fast (SF)



        - Accounting
            - Balance
            # View Account Balance via CLI
                % The account balance (put link to accounts balance) can be inspected with the following command under CLI 

            - Statement
            # View Usage Statistics
                % The usage statistics of each cluster, in terms of number of CPU hours consumed and charges incurred, can be inspected with the following command under CLI 
            

####################################################################################################################################

        - Actions
            - Create
            # Create New Job
                % A new Job can be created by assembling all simulation input files and batch script together in the same folder under the cluster home directory 

            - Submit
            # Submit Job
                % A job can then be submitted to the relevant queue for its execution with the qsub command 
            

            - Check status
            # Check status of Job
                % The status of the job can be checkd at any moment with the qstat command 
                % Possible statuses might include Running, Pending, Error, Aborted etc....

            - Terminate
            # Terminate
                % A Job might be terminated artificially by the user before its completion with the qdel command 

####################################################################################################################################